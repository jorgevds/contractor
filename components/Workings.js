import styles from "./workings.module.css";

const Workings = () => {
  return (
    <div className={styles.container}>
      <h1>nothing to see here for now</h1>
      <h3>
        this component is here so that the footer sticks to the bottom of the
        viewport
      </h3>
    </div>
  );
};

export default Workings;
